# Plan: Broen — one row on ink in lit water

Spec: `docs/superpowers/specs/2026-09-12-broen-design.md`. Branch `feat/broen` in its own
worktree from `7d3c851`. Every task: test first where there is a pure part, then the code,
then the suite; one commit per task.

1. **The ink's `over` mode** — `lib/ink.ts`. `InkPalette.over`, `.rain`, `.clear`. Context
   with alpha in over mode; pigments passed through as colour (export `preparePigments`);
   splat adds `vec4(colour·amount, amount)`; the show pass emits premultiplied colour and
   alpha; live drops rain from the top; dye dissipation from `clear`. Tests: preparation
   per mode, `createInk` still declines without WebGL2.
2. **The floor and the inks** — `lib/film.ts`: `BRIDGE` scene, `film.bridge = floorAt(...)`,
   `film.drops`; `FilmWater` gains `'bridge'`, `FilmInk` gains `'drops'`. `wash.module.css`:
   `.bridge` with ground and still. Tests in `film.test.ts` and `ground.test.ts`.
3. **The content** — `content/site.no.ts`: `date: string | null` on every item, `day` and
   `month` removed, `happenings.undated` strings, the comment rewritten to say why.
4. **The section** — `Happenings.tsx` + `happenings.module.css`: head on white, the pool
   with the box and two canvases, one sorted row, today on the client, the rail as before.
   `Happenings.test.tsx` rewritten to the new claims.
5. **The e2e** — `sections.spec.ts`: order, box painted, sorted row with today, links,
   phone rail. `lineAgainstPegs` and the above/below-the-line block go with the axis.
6. **Look, measure, gate** — build, serve on a spare port, real-GPU screenshots desktop
   and phone with both canvases painting, frame cost, vitest/tsc/eslint/e2e. Then the PR.
