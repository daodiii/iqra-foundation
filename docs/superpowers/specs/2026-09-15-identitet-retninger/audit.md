# Audit: the brief's sections → where they are carried → what proves it

| Brief | Page / element | Content key | Test |
|---|---|---|---|
| 1 Posisjonering — «Kunnskap \| Dialog \| Møteplasser \| Samfunnsdeltakelse» | the four areas as the through-line on `/`, the sections of `/vart-arbeid`, the red thread in the footer | `brief.four`, `brief.areas[]`, `brief.thread` | `content/brief.test.ts` (verbatim, both directions); `e2e/pages.spec.ts` «the home page…», «vårt arbeid…»; `e2e/shell.spec.ts` «the footer carries…» |
| 2 Hovedtekst på forsiden — headline, paragraph, «Utforsk vårt arbeid \| Støtt oss» | `/` h1, lede, the two buttons → `/vart-arbeid`, `/stott-oss` | `brief.home.headline/paragraph/buttons`, `site.cta` | `content/brief.test.ts`; `e2e/pages.spec.ts` «the home page…» |
| 3 Visjon | `/` Visjon | `brief.vision.headline/paragraph` | same |
| 4 Misjon | `/` Misjon | `brief.mission.headline/paragraph` (also the description of `/vart-arbeid`) | same |
| 5 Om oss | `/om-oss` h1 + four paragraphs; the name's story (the user's words) under it | `brief.about.title/paragraphs`, `site.pages.about.story` | `content/brief.test.ts`; `e2e/pages.spec.ts` «om oss…» |
| 6 Fire hovedområder | `/vart-arbeid` four sections `#kunnskap #dialog #moteplasser #samfunnsdeltakelse`; the four on `/` linking to them | `brief.areas[]` | `content/brief.test.ts`; `e2e/pages.spec.ts` «vårt arbeid…» |
| 7 Menneskene bak Iqra | `/menneskene-bak` h1 + the board paragraph; the `menneskene` collection (empty) with the honest line | `brief.people.title/paragraph`, `site.pages.people.empty`, `lib/content.ts getPeople` | `content/brief.test.ts`; `lib/content.test.ts` «menneskene»; `e2e/pages.spec.ts` «menneskene bak…» |
| 8 Meny og undersider — the nine items, Støtt oss a button; own pages for Menneskene bak, Arrangementer, Ressurser, Styringsdokumenter, Kontakt, Støtt oss; what the two pages will hold | header + footer nav; `/arrangementer` (kommende/tidligere), `/ressurser`, `/styringsdokumenter`, `/kontakt`, `/stott-oss`; the two page sentences as ledes and descriptions | `brief.menu[]` → `site.nav`, `brief.documents`, `brief.resources`, the collections | `e2e/shell.spec.ts` (nine items at 1440/1280/1024, the drawer on a phone, the current page); `e2e/pages.spec.ts` (each page's title, description, empty state); `lib/content.test.ts`; `content/collections.test.ts` (Keystatic writes where the site reads) |
| 9 Iqra-identiteten — logo, colours, typography used consistently; the name's meaning may stay as a story; the red thread | the tokens (`app/globals.css`), the logo per ground (`components/site/Logo.tsx`, `public/brand/`), the fonts (`app/fonts.ts`), the thread in the footer, the favicon and the share card | `brief.thread`, `site.pages.about.story` | `e2e/shell.spec.ts` «the footer carries…», «the header carries the logo…»; `e2e/pages.spec.ts` «the share card…»; `lib/…` |
| Nothing invented | every collection empty; the four bracketed facts | `site.contact`, `site.support` | `scripts/lib/content-check.test.ts` «the four facts … are the only placeholders»; `node scripts/check-content.mjs` |
